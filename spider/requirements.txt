# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Scraping. Transfermarkt sits behind AWS WAF (captcha), so a plain HTTP
# client is not enough — we drive a real (patched) Chromium and reuse the
# aws-waf-token across requests. httpx is kept for lightweight internal use.
httpx==0.28.1
patchright==1.60.1
selectolax==0.3.27
tenacity==9.0.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Object storage (rustfs is S3-compatible)
aioboto3==13.2.0

# Config / validation
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
